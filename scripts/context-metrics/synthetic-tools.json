{
  "description": "Synthetic tool definitions for scaling tests. These tools are not real - they exist only to inflate tool definition context size.",
  "servers": [
    {
      "serverSlug": "mock-crm",
      "tools": [
        {
          "name": "create_contact",
          "description": "Creates a new contact in the CRM system with the specified details.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "firstName": { "type": "string", "description": "Contact's first name" },
              "lastName": { "type": "string", "description": "Contact's last name" },
              "email": { "type": "string", "description": "Contact's email address" },
              "phone": { "type": "string", "description": "Contact's phone number" },
              "company": { "type": "string", "description": "Company name" }
            },
            "required": ["firstName", "lastName", "email"]
          }
        },
        {
          "name": "update_contact",
          "description": "Updates an existing contact's information in the CRM.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "contactId": { "type": "string", "description": "Unique contact identifier" },
              "firstName": { "type": "string" },
              "lastName": { "type": "string" },
              "email": { "type": "string" },
              "phone": { "type": "string" }
            },
            "required": ["contactId"]
          }
        },
        {
          "name": "delete_contact",
          "description": "Permanently deletes a contact from the CRM system.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "contactId": { "type": "string", "description": "Unique contact identifier" }
            },
            "required": ["contactId"]
          }
        },
        {
          "name": "list_contacts",
          "description": "Lists all contacts with optional filtering and pagination.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "filter": { "type": "string", "description": "Search filter query" },
              "limit": { "type": "number", "description": "Maximum results to return" },
              "offset": { "type": "number", "description": "Pagination offset" }
            }
          }
        },
        {
          "name": "get_contact",
          "description": "Retrieves detailed information about a specific contact.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "contactId": { "type": "string", "description": "Unique contact identifier" }
            },
            "required": ["contactId"]
          }
        },
        {
          "name": "create_deal",
          "description": "Creates a new deal/opportunity in the sales pipeline.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "title": { "type": "string", "description": "Deal title" },
              "value": { "type": "number", "description": "Deal value in USD" },
              "contactId": { "type": "string", "description": "Associated contact" },
              "stage": { "type": "string", "enum": ["lead", "qualified", "proposal", "negotiation", "closed"] }
            },
            "required": ["title", "value"]
          }
        },
        {
          "name": "update_deal",
          "description": "Updates an existing deal's information or stage.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "dealId": { "type": "string" },
              "title": { "type": "string" },
              "value": { "type": "number" },
              "stage": { "type": "string" }
            },
            "required": ["dealId"]
          }
        },
        {
          "name": "list_deals",
          "description": "Lists all deals with filtering by stage, value, or contact.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "stage": { "type": "string" },
              "minValue": { "type": "number" },
              "maxValue": { "type": "number" },
              "contactId": { "type": "string" }
            }
          }
        },
        {
          "name": "create_task",
          "description": "Creates a task associated with a contact or deal.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "title": { "type": "string" },
              "description": { "type": "string" },
              "dueDate": { "type": "string", "format": "date-time" },
              "contactId": { "type": "string" },
              "dealId": { "type": "string" },
              "priority": { "type": "string", "enum": ["low", "medium", "high"] }
            },
            "required": ["title"]
          }
        },
        {
          "name": "complete_task",
          "description": "Marks a task as completed.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "taskId": { "type": "string" }
            },
            "required": ["taskId"]
          }
        }
      ]
    },
    {
      "serverSlug": "mock-analytics",
      "tools": [
        {
          "name": "query_events",
          "description": "Queries analytics events with filters and aggregations.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "eventType": { "type": "string" },
              "startDate": { "type": "string", "format": "date-time" },
              "endDate": { "type": "string", "format": "date-time" },
              "groupBy": { "type": "array", "items": { "type": "string" } },
              "filters": { "type": "object" }
            },
            "required": ["startDate", "endDate"]
          }
        },
        {
          "name": "get_metrics",
          "description": "Retrieves computed metrics for a time period.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "metrics": { "type": "array", "items": { "type": "string" } },
              "startDate": { "type": "string" },
              "endDate": { "type": "string" },
              "granularity": { "type": "string", "enum": ["hour", "day", "week", "month"] }
            },
            "required": ["metrics", "startDate", "endDate"]
          }
        },
        {
          "name": "create_dashboard",
          "description": "Creates a new analytics dashboard.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "name": { "type": "string" },
              "description": { "type": "string" },
              "widgets": { "type": "array", "items": { "type": "object" } }
            },
            "required": ["name"]
          }
        },
        {
          "name": "list_dashboards",
          "description": "Lists all available dashboards.",
          "inputSchema": {
            "type": "object",
            "properties": {}
          }
        },
        {
          "name": "get_funnel",
          "description": "Calculates conversion funnel metrics.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "steps": { "type": "array", "items": { "type": "string" } },
              "startDate": { "type": "string" },
              "endDate": { "type": "string" }
            },
            "required": ["steps", "startDate", "endDate"]
          }
        },
        {
          "name": "get_retention",
          "description": "Calculates user retention cohort analysis.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "cohortType": { "type": "string", "enum": ["daily", "weekly", "monthly"] },
              "startDate": { "type": "string" },
              "periods": { "type": "number" }
            },
            "required": ["cohortType", "startDate"]
          }
        },
        {
          "name": "export_report",
          "description": "Exports analytics data to CSV or JSON format.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "reportId": { "type": "string" },
              "format": { "type": "string", "enum": ["csv", "json"] },
              "dateRange": { "type": "object" }
            },
            "required": ["reportId", "format"]
          }
        },
        {
          "name": "create_segment",
          "description": "Creates a user segment based on behavioral criteria.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "name": { "type": "string" },
              "conditions": { "type": "array", "items": { "type": "object" } }
            },
            "required": ["name", "conditions"]
          }
        },
        {
          "name": "list_segments",
          "description": "Lists all defined user segments.",
          "inputSchema": {
            "type": "object",
            "properties": {}
          }
        },
        {
          "name": "get_segment_size",
          "description": "Returns the current size of a user segment.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "segmentId": { "type": "string" }
            },
            "required": ["segmentId"]
          }
        }
      ]
    },
    {
      "serverSlug": "mock-storage",
      "tools": [
        {
          "name": "upload_file",
          "description": "Uploads a file to cloud storage.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "bucket": { "type": "string" },
              "path": { "type": "string" },
              "content": { "type": "string" },
              "contentType": { "type": "string" }
            },
            "required": ["bucket", "path", "content"]
          }
        },
        {
          "name": "download_file",
          "description": "Downloads a file from cloud storage.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "bucket": { "type": "string" },
              "path": { "type": "string" }
            },
            "required": ["bucket", "path"]
          }
        },
        {
          "name": "delete_file",
          "description": "Deletes a file from cloud storage.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "bucket": { "type": "string" },
              "path": { "type": "string" }
            },
            "required": ["bucket", "path"]
          }
        },
        {
          "name": "list_files",
          "description": "Lists files in a storage bucket with optional prefix filter.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "bucket": { "type": "string" },
              "prefix": { "type": "string" },
              "limit": { "type": "number" }
            },
            "required": ["bucket"]
          }
        },
        {
          "name": "create_bucket",
          "description": "Creates a new storage bucket.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "name": { "type": "string" },
              "region": { "type": "string" },
              "publicAccess": { "type": "boolean" }
            },
            "required": ["name"]
          }
        },
        {
          "name": "delete_bucket",
          "description": "Deletes a storage bucket and all its contents.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "name": { "type": "string" },
              "force": { "type": "boolean" }
            },
            "required": ["name"]
          }
        },
        {
          "name": "get_signed_url",
          "description": "Generates a signed URL for temporary file access.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "bucket": { "type": "string" },
              "path": { "type": "string" },
              "expiresIn": { "type": "number" },
              "operation": { "type": "string", "enum": ["read", "write"] }
            },
            "required": ["bucket", "path"]
          }
        },
        {
          "name": "copy_file",
          "description": "Copies a file within or between buckets.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "sourceBucket": { "type": "string" },
              "sourcePath": { "type": "string" },
              "destBucket": { "type": "string" },
              "destPath": { "type": "string" }
            },
            "required": ["sourceBucket", "sourcePath", "destBucket", "destPath"]
          }
        },
        {
          "name": "get_file_metadata",
          "description": "Retrieves metadata for a stored file.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "bucket": { "type": "string" },
              "path": { "type": "string" }
            },
            "required": ["bucket", "path"]
          }
        },
        {
          "name": "set_file_metadata",
          "description": "Updates metadata for a stored file.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "bucket": { "type": "string" },
              "path": { "type": "string" },
              "metadata": { "type": "object" }
            },
            "required": ["bucket", "path", "metadata"]
          }
        }
      ]
    },
    {
      "serverSlug": "mock-email",
      "tools": [
        {
          "name": "send_email",
          "description": "Sends a transactional email to one or more recipients.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "to": { "type": "array", "items": { "type": "string" } },
              "cc": { "type": "array", "items": { "type": "string" } },
              "bcc": { "type": "array", "items": { "type": "string" } },
              "subject": { "type": "string" },
              "body": { "type": "string" },
              "html": { "type": "boolean" },
              "attachments": { "type": "array", "items": { "type": "object" } }
            },
            "required": ["to", "subject", "body"]
          }
        },
        {
          "name": "create_template",
          "description": "Creates an email template with variable placeholders.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "name": { "type": "string" },
              "subject": { "type": "string" },
              "htmlBody": { "type": "string" },
              "textBody": { "type": "string" },
              "variables": { "type": "array", "items": { "type": "string" } }
            },
            "required": ["name", "subject", "htmlBody"]
          }
        },
        {
          "name": "send_template",
          "description": "Sends an email using a predefined template.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "templateId": { "type": "string" },
              "to": { "type": "array", "items": { "type": "string" } },
              "variables": { "type": "object" }
            },
            "required": ["templateId", "to"]
          }
        },
        {
          "name": "list_templates",
          "description": "Lists all available email templates.",
          "inputSchema": {
            "type": "object",
            "properties": {}
          }
        },
        {
          "name": "get_delivery_status",
          "description": "Gets the delivery status of a sent email.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "messageId": { "type": "string" }
            },
            "required": ["messageId"]
          }
        },
        {
          "name": "list_sent_emails",
          "description": "Lists recently sent emails with delivery stats.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "startDate": { "type": "string" },
              "endDate": { "type": "string" },
              "status": { "type": "string", "enum": ["delivered", "bounced", "failed"] }
            }
          }
        },
        {
          "name": "add_to_suppression_list",
          "description": "Adds an email address to the suppression list.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "email": { "type": "string" },
              "reason": { "type": "string" }
            },
            "required": ["email"]
          }
        },
        {
          "name": "remove_from_suppression_list",
          "description": "Removes an email address from the suppression list.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "email": { "type": "string" }
            },
            "required": ["email"]
          }
        },
        {
          "name": "get_bounce_rate",
          "description": "Gets email bounce rate statistics.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "startDate": { "type": "string" },
              "endDate": { "type": "string" }
            },
            "required": ["startDate", "endDate"]
          }
        },
        {
          "name": "verify_email",
          "description": "Verifies if an email address is valid and deliverable.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "email": { "type": "string" }
            },
            "required": ["email"]
          }
        }
      ]
    },
    {
      "serverSlug": "mock-payments",
      "tools": [
        {
          "name": "create_charge",
          "description": "Creates a one-time charge for a customer.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "amount": { "type": "number" },
              "currency": { "type": "string" },
              "customerId": { "type": "string" },
              "description": { "type": "string" },
              "paymentMethod": { "type": "string" }
            },
            "required": ["amount", "currency", "customerId"]
          }
        },
        {
          "name": "refund_charge",
          "description": "Refunds a previously created charge.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "chargeId": { "type": "string" },
              "amount": { "type": "number" },
              "reason": { "type": "string" }
            },
            "required": ["chargeId"]
          }
        },
        {
          "name": "create_subscription",
          "description": "Creates a recurring subscription for a customer.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "customerId": { "type": "string" },
              "priceId": { "type": "string" },
              "trialDays": { "type": "number" }
            },
            "required": ["customerId", "priceId"]
          }
        },
        {
          "name": "cancel_subscription",
          "description": "Cancels an active subscription.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "subscriptionId": { "type": "string" },
              "immediately": { "type": "boolean" }
            },
            "required": ["subscriptionId"]
          }
        },
        {
          "name": "list_subscriptions",
          "description": "Lists all subscriptions for a customer.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "customerId": { "type": "string" },
              "status": { "type": "string", "enum": ["active", "canceled", "past_due"] }
            }
          }
        },
        {
          "name": "create_customer",
          "description": "Creates a new customer in the payment system.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "email": { "type": "string" },
              "name": { "type": "string" },
              "metadata": { "type": "object" }
            },
            "required": ["email"]
          }
        },
        {
          "name": "update_customer",
          "description": "Updates customer information.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "customerId": { "type": "string" },
              "email": { "type": "string" },
              "name": { "type": "string" }
            },
            "required": ["customerId"]
          }
        },
        {
          "name": "list_invoices",
          "description": "Lists all invoices for a customer.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "customerId": { "type": "string" },
              "status": { "type": "string" },
              "limit": { "type": "number" }
            }
          }
        },
        {
          "name": "create_payment_link",
          "description": "Creates a payment link for checkout.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "priceId": { "type": "string" },
              "quantity": { "type": "number" },
              "successUrl": { "type": "string" },
              "cancelUrl": { "type": "string" }
            },
            "required": ["priceId", "successUrl"]
          }
        },
        {
          "name": "get_balance",
          "description": "Gets the current account balance.",
          "inputSchema": {
            "type": "object",
            "properties": {}
          }
        }
      ]
    },
    {
      "serverSlug": "mock-notifications",
      "tools": [
        {
          "name": "send_push",
          "description": "Sends a push notification to a device or user.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "userId": { "type": "string" },
              "deviceToken": { "type": "string" },
              "title": { "type": "string" },
              "body": { "type": "string" },
              "data": { "type": "object" }
            },
            "required": ["title", "body"]
          }
        },
        {
          "name": "send_sms",
          "description": "Sends an SMS message to a phone number.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "to": { "type": "string" },
              "message": { "type": "string" }
            },
            "required": ["to", "message"]
          }
        },
        {
          "name": "create_notification_channel",
          "description": "Creates a notification channel for organizing notifications.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "name": { "type": "string" },
              "description": { "type": "string" },
              "importance": { "type": "string", "enum": ["low", "default", "high"] }
            },
            "required": ["name"]
          }
        },
        {
          "name": "schedule_notification",
          "description": "Schedules a notification for future delivery.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "userId": { "type": "string" },
              "title": { "type": "string" },
              "body": { "type": "string" },
              "scheduledAt": { "type": "string", "format": "date-time" }
            },
            "required": ["userId", "title", "body", "scheduledAt"]
          }
        },
        {
          "name": "cancel_scheduled_notification",
          "description": "Cancels a scheduled notification.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "notificationId": { "type": "string" }
            },
            "required": ["notificationId"]
          }
        },
        {
          "name": "register_device",
          "description": "Registers a device for push notifications.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "userId": { "type": "string" },
              "deviceToken": { "type": "string" },
              "platform": { "type": "string", "enum": ["ios", "android", "web"] }
            },
            "required": ["userId", "deviceToken", "platform"]
          }
        },
        {
          "name": "unregister_device",
          "description": "Unregisters a device from push notifications.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "deviceToken": { "type": "string" }
            },
            "required": ["deviceToken"]
          }
        },
        {
          "name": "get_notification_stats",
          "description": "Gets notification delivery statistics.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "startDate": { "type": "string" },
              "endDate": { "type": "string" },
              "channel": { "type": "string" }
            }
          }
        },
        {
          "name": "list_scheduled_notifications",
          "description": "Lists all scheduled notifications.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "userId": { "type": "string" },
              "status": { "type": "string" }
            }
          }
        },
        {
          "name": "update_user_preferences",
          "description": "Updates notification preferences for a user.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "userId": { "type": "string" },
              "pushEnabled": { "type": "boolean" },
              "smsEnabled": { "type": "boolean" },
              "emailEnabled": { "type": "boolean" }
            },
            "required": ["userId"]
          }
        }
      ]
    },
    {
      "serverSlug": "mock-search",
      "tools": [
        {
          "name": "search",
          "description": "Performs a full-text search across indexed content.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "query": { "type": "string" },
              "index": { "type": "string" },
              "filters": { "type": "object" },
              "limit": { "type": "number" },
              "offset": { "type": "number" }
            },
            "required": ["query", "index"]
          }
        },
        {
          "name": "index_document",
          "description": "Adds or updates a document in the search index.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "index": { "type": "string" },
              "id": { "type": "string" },
              "document": { "type": "object" }
            },
            "required": ["index", "id", "document"]
          }
        },
        {
          "name": "delete_document",
          "description": "Removes a document from the search index.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "index": { "type": "string" },
              "id": { "type": "string" }
            },
            "required": ["index", "id"]
          }
        },
        {
          "name": "create_index",
          "description": "Creates a new search index with specified mappings.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "name": { "type": "string" },
              "mappings": { "type": "object" },
              "settings": { "type": "object" }
            },
            "required": ["name"]
          }
        },
        {
          "name": "delete_index",
          "description": "Deletes a search index and all its documents.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "name": { "type": "string" }
            },
            "required": ["name"]
          }
        },
        {
          "name": "get_suggestions",
          "description": "Gets autocomplete suggestions for a partial query.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "prefix": { "type": "string" },
              "index": { "type": "string" },
              "limit": { "type": "number" }
            },
            "required": ["prefix", "index"]
          }
        },
        {
          "name": "bulk_index",
          "description": "Indexes multiple documents in a single operation.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "index": { "type": "string" },
              "documents": { "type": "array", "items": { "type": "object" } }
            },
            "required": ["index", "documents"]
          }
        },
        {
          "name": "get_index_stats",
          "description": "Gets statistics about a search index.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "index": { "type": "string" }
            },
            "required": ["index"]
          }
        },
        {
          "name": "reindex",
          "description": "Rebuilds a search index from source data.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "sourceIndex": { "type": "string" },
              "destIndex": { "type": "string" }
            },
            "required": ["sourceIndex", "destIndex"]
          }
        },
        {
          "name": "analyze_text",
          "description": "Analyzes text using the index's configured analyzer.",
          "inputSchema": {
            "type": "object",
            "properties": {
              "index": { "type": "string" },
              "text": { "type": "string" },
              "analyzer": { "type": "string" }
            },
            "required": ["index", "text"]
          }
        }
      ]
    }
  ]
}
