# Act configuration for local GitHub Actions testing
# See: https://github.com/nektos/act

# Use medium-sized runner image (includes more tools than micro)
-P ubuntu-latest=catthehacker/ubuntu:act-latest

# Specify Apple M-series chip compatible container architecture
--container-architecture linux/amd64

# Reuse containers to avoid hanging on startup
--reuse

# Don't pull images every time (use cached)
--pull=false